# Result of autodetection

`secundo pack <dir>` should print something like:

```yaml
Detected: TypeScript project
Interpreter: ts-node
Entrypoint: src/main.ts
App ID: com.example.myapp
Version: 1.0.0
Output: myapp.sec
```
